Flask==2.3.3
Flask-SocketIO==5.3.6
catt==0.12.11
PyChromecast==13.0.8
Pillow==10.0.1
python-socketio==5.9.0
python-engineio==4.7.1
pathlib
rumps==0.4.0
py2app==0.28.6
packaging
Flask-Cors==4.0.1
gevent
gevent-websocket
samsungtvws==3.0.5
# py2app 0.28.6 needs an older setuptools to build, for two separate reasons:
#   - setuptools 70+ dropped the pkg_resources.packaging shim that py2app's
#     matplotlib recipe imports (ImportError at build time)
#   - setuptools 71+ ships its vendored deps with real .dist-info dirs, which
#     collide with the standalone importlib_metadata that Flask pulls in on
#     Python 3.9 ("File exists: ...importlib_metadata-8.7.1.dist-info")
# 69.5.1 is the newest version that avoids both.
setuptools==69.5.1
